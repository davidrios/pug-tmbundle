!!! 5
- var variable = "<em>some text</em>"
-var var2 = 'no spaces after -'
_test  =  [{abc: "lalal"}]
name = function() {}

.98349 lala
._[ddsf lala

//  oisdfjoiasdjf
  pasdf.
    siodfaj sodifjaosdifj asoidfjiasjdf
    aiosd jfoaisjd foiajsdofij

_iv:asd-_FD: _Fsadf tag name weird &amp;characters
asdf:    ab:[this should be text
#{"tag" + "_iname"} some tag with an iterpolated name

[this is rendered as standalone text
.=this too
##this too
.#this too
"this too"
#{this too

sub: tag: thisisavar = 'var_text_55'
= 'as #{1+1}demonstrated here: ' + thisisavar

sub2: tag2: -console.log('unbuffered js inside subtag?')

sdaf#-tagid.-sdfa oidsjfoijdf

div(sdf-+a\||./asd=123,   type="te\x98t\u8876",lol,
  lol
  lol, abc = _test, fdg=console.log(123, (1 + (1 + 2)), undefined || true && "sdf#{1+1}asdf")
  asdf, bcd=[123, [_test, {asdf: console.log(123)}]], afd={a: 1+1, 'b': {c: (null)}})= _test
include page
html
  mixin lala(abc, lala)
    div.lala-output
      p.abc= null + abc
      p.lala= lala

  mixin lala((123+{asdf:(1+1)}), "mixin long call")after long mixin use

  .test
    +lala(123, "mixin + call 1")after + mixin use

  .test2+lala(123, "mixin + call 2")

  body
    head
      script#idscript.script(type="text/javascript",   qwerqwer)(test="123", lol=(1+
        1),  other)
        var la = lala;
    
        function script(arg1, arg2) {
          console.log("string", arg1, arg2);
        }

        script(la, "string1")

        console.log(script(la, "string3"));

        var lala = 0;

        var obj = {abc: 123};

      style(type="text/css")
        .lala.lala {
          display: none;
          float: left;
          color: #000;
        }

        div.saf {
          display: none;
        }

      link(rel="stylesheet", href="asdiofjs.css")

    block lala
      div.lala-block
      yield

    div.lala-block-test
      block prepend lala
        div.lala-prepend line1
        | line2

      append lala
        div.lala-append line1
        | line2
        yield

    for i in [1,2,3,null]
      div= i

    h1 title

    p= console.log("asdf")

    label
      | Username:
      input(type="text")

    label Username:
      input(type="text")

    tag: a dsaoi
      lala lili

    p#lala.lele.lele!= variable

    #lala.lelel.lele= variable

    #test(abc="lala", lele=9, lili=variable) some text

    p.variable-output
      = variable

      != variable + "concatenated text"

    .class some text with #{variable + "&"} and !{variable}} vars \#{this will show}
    .class.
      line-break on interpolation won't work #{ 1 + (
      1 - 1) }

    p some text  // not some comment

    a(href="lala", something=variable,) some link

    sdafjosidafj.ajiosd fja


      //
        multiline-comment
        sdafu&iahsdf

        asdfjioasdj
    asdf

        dsfasdf
      asdf

        asdfasdfa

        asdfasdfa

    odisfjosdijf

    //
      comment
      sdafasdf

    //
      sdafsdf

extra-space-after:  ajsiodf:   b= 9 + 9

  p: a aosdifjoasidjf

  p: [a ds
    multi-line.
      aoisdjfpasd
      asoidfjiasjdf

    asdfasdf: sadfasd: asdfsdf

  p: a:.a asdfjij
    sdf

  strange[sometext
  strange_sometext- lallslla
  tag=variable
  strange[sometext

    //-
      asjdiofajsodijfao
      asdfjaosidjfoij

      osdifjaosidjf

  div: p some text

    div.class.another: a(href=variable, test="la\"sdafla"): p.red(style="")some text

      div.
        oai sjdofijasdoifj

    div.class.another: p.red(style="").asdfsd(lol=123)#ohno.
      some text dasfpoasdf
      asdjiofa jsodfijas
      aosid fjoasidjf oaijsd
      oiasjd foaisjdfoij#{null}

    p some text.
      this should be a tag

  multi line #{"asdfsdf" + variable}

    p
      multi.line allasdkfj
      | line of text #{variable + "aosidjfsd" + 0 + undefined} some other text
      | more pure text

    script(type="text/javascript")
      var a = 1;

      function lala() {
        console.log(a);
      }

    a= variable
    //asdoifjaois
      ajsdiofja
      asodifjoasidj

      sdfasdf
      a
      asdfasdf

        asdfasdf
            asdfasdf

      sdfasdf


  asdfasdfasd
    asdfasdf

a.p.p

p.lala sodifjoij + 9
